# UIHarness (Core)

## Setup

```bash
yarn add -D @uiharness/core
```

Add scripts to `package.json`:

```json
{
  "scripts": {
    "start": "uiharness start",
    "dist": "uiharness dist",
    "serve": "uiharness serve"
  }
}
```
