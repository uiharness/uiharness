{
  "name": "uiharness.website.server",
  "version": "0.0.9",
  "scripts": {
    "ssr": "ssr $@",
    "now": "now dev --listen 8080",
    "deploy": "now --prod",
    "start": "ts-node -T src/server",
    "watch": "nodemon --watch ../src -e ts,tsx --delay 2"
  },
  "dependencies": {
    "@platform/react.ssr": "^1.1.37"
  },
  "devDependencies": {
    "ts-node": "^8.5.4"
  },
  "license": "MIT",
  "private": true
}
