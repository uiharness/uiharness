#
# Sample config for unit-tests.
# Non-default values.
#

name: my-app

electron:
  port: 1234
  entry:
    main: ./foo/start.ts
    renderer: foo/render.tsx
  bundle:
    sourcemaps: false
    treeshake: true
    logLevel: 1

web:
  port: 3030
  entry: ./foo/web.html
  bundle:
    sourcemaps: false
    treeshake: true
    logLevel: 1
