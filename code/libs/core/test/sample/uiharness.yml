#
# Sample config for unit-tests.
# Non-default values.
#

electron:
  port: 1234
  entry:
    main: ./foo/main/start.ts
    renderer: ./foo/renderer/index.html
  bundle:
    sourcemaps: false
    treeshake: true

web:
  port: 3030
  entry: ./foo/web/hello.html
  bundle:
    sourcemaps: false
    treeshake: true
