{
  "name": "example",
  "version": "1.0.0",
  "description": "Example usage of @uiharess/react",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "postinstall": "uiharness init",
    "build": "node ./node_modules/typescript/bin/tsc --importHelpers --project tsconfig.lib.json $@",
    "debug:sync-libs": "msync sl && sh ./sh/copy-bin.sh",
    "reset": "echo DEBUG REST && yarn debug:sync-libs && uiharness debug:reset",
    "tmp": "yarn debug:reset && sleep 0.5s && yarn postinstall",
    "start": "uiharness start",
    "bundle": "uiharness bundle",
    "serve": "serve -s build"
  },
  "dependencies": {},
  "devDependencies": {
    "@uiharness/react": "1.0.7"
  },
  "repository": "https://github.com/uiharness/uiharness",
  "license": "UNLICENSED",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "private": true
}